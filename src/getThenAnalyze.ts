/**
 * 0. specify channel (channel ID)
 * 1. list up all videos in a channel (`listAllVideos`)
 * 2. map video to comments (video.forEach `getThenSaveComments`)
 * 3. analysis (what kind of?)
 *   monthly continuation rate
 */
